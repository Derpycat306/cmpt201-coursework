#define _POSIX_C_SOURCE 200809L
#include <stdio.h>

int main() {
  char *strings[5];
  char count = 0;

  while (1) {
    printf("Enter input: ");
    char *line = NULL;
    size_t len = 0;
    
    if(getline(&line, &len, stdin) == -1)break;

    strings[count] = line;
  }
}
