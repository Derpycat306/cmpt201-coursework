#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
  char *str = NULL;
  size_t size = 0;
  ssize_t len;

  len = getline(&str, &size, stdin);

  if (len == -1) {
    printf("Error reading line");
    return 1;
  }

  char *parse = str;

  char *token;
  while ((token = strtok_r(parse, " ", &parse))) {
    printf("%s\n", token);
  }

  free(str);
  return 0;
}
