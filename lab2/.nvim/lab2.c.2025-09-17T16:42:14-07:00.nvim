#include <stdio.h>
#include <unistd.h>
#include <sys/wait.h>
int main(){
  while(1){
    int pid = fork();
    if(pid){
      int wstatus;
      wait(&wstatus);
    }else{
      char input[50];
      scanf("%s", input);
      execl(input, input, (char*) NULL);
    }
  }
}
