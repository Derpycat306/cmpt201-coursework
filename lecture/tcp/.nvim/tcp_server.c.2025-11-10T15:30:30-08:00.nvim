#include <stdio.h>
#include <stdlib.h>
#include <sys/socket.h>

int main() {
  printf("Server");

  // socket
  int socket_fd = socket(AF_UNIX, SOCK_STREAM, 0);
  if (socket_fd == -1) {
    perror("socket");
    exit(EXIT_FAILURE);
  }

  // bind
  int b = bind(socket_fd,);

  // listen


  // accept


  // read


  // close
  return 0;
}
