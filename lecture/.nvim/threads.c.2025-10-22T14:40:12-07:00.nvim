#include <pthread.h>
#include <stdio.h>
#include <unistd.h>

void *func(void *arg) {
  sleep(1);
  printf("hello, I am a thread.\n");
  return NULL;
}

int main(void) {
  pthread_t thread1;
  pthread_create(&thread1, NULL, func, NULL);
  pthread_detatch(&thread1);
  printf("done\n");
  return 0;
}
